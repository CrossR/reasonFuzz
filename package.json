{
  "name": "reasonfuzz",
  "version": "0.0.1",
  "description": "Fuzzy string matching for ReasonML.",
  "esy": {
    "build": "refmterr dune build --root . -j4",
    "install": [
      "esy-installer ReasonFuzz.install",
      "esy-installer Matchers.install"
    ]
  },
  "scripts": {
    "test": "esy @test x RunTests",
    "format": "esy dune build @fmt --auto-promote"
  },
  "dependencies": {
    "ocaml": "^4.7.0",
    "@esy-ocaml/reason": "*",
    "@opam/dune": ">=1.6.0",
    "refmterr": "*"
  },
  "devDependencies": {
    "@opam/merlin": "*",
    "@reason-native/rely": "^1.2.0",
    "@reason-native/console": "^0.0.3",
    "pesy": "*",
    "reperf": "^1.4.0"
  }
}
